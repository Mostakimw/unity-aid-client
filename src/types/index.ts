export type TDonation = {
  donationId: string;
  image: string;
  title: string;
  category: string;
  amount: number;
  description: string;
  timestamp: string;
  isDeleted: boolean;
  totalReceived: number;
};
